{:objects
 {:pin
  {:description "An IO pin."
   :fields
   {:id {:type (non-null ID)}
    :board_value {:type Boolean}}}}
 :queries
 {:pin_by_id
  {:type :pin
   :description "The pins on the Raspberry Pi."
   :args {:id {:type ID}}
   :resolve :query/pin_by_id
   }}}
